spring:
  cloud:
    zookeeper:
      connect-string: 192.168.3.201:2181
      discovery:
        register: false

# 服务名称
provider:
    ribbon:
    # 负载策略
      NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule